package com.example.android.baking.data.struct;

/*
public class ModelAdapters {

    @TypeConverter
    @ToJson
    public String localDateTimeToString(LocalDateTime localDateTime) {
        return (localDateTime == null) ? null : DateTimeFormatter.ISO_LOCAL_DATE_TIME.format(localDateTime);
    }

    @TypeConverter
    @FromJson
    public LocalDateTime localDateTimeFromString(String localDateTime) {
        return (localDateTime == null) ? null : LocalDateTime.parse(localDateTime, DateTimeFormatter.ISO_LOCAL_DATE_TIME);
    }
}
*/
